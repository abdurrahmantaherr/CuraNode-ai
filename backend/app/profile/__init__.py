"""Patient profile (FR2): basic details, allergies, chronic conditions, and
current medications. The JSON API (`router.py`) and the server-rendered pages
(`web/router.py`) both call `service.py`; no business rule lives twice."""
